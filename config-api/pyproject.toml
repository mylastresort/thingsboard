[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "config-api"
version = "1.0.0"
description = "Configuration API for ThingsBoard custom stack"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic",
]
