services:
  database:
    image: postgres:16
    container_name: database
    restart: on-failure:3
    # mem_limit: 1g
    # cpus: 1.0
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - ../psql_data/main:/var/lib/postgresql/data
      - .:/app
    networks:
      - thingsboard
    env_file:
      - ./.env
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "max_connections=50"
      - "-c"
      - "work_mem=16MB"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready",
          "-d",
          "${POSTGRES_DB}",
          "-U",
          "${POSTGRES_USER}",
        ]
      interval: 2m
      timeout: 30s
      retries: 3
      start_period: 80s

  # Commented out - uncomment only when needed
  # anomalies-script:
  #   build:
  #     context: ./send-telemetry/
  #     dockerfile: anomalies-script.Dockerfile
  #   container_name: anomalies-script
  #   mem_limit: 512m
  #   cpus: 0.5
  #   depends_on:
  #     thingsboard:
  #       condition: service_healthy
  #   networks:
  #     - thingsboard

  # forecast-script:
  #   build:
  #     context: ./send-telemetry/
  #     dockerfile: forecast-script.Dockerfile
  #   container_name: forecast-script
  #   mem_limit: 512m
  #   cpus: 0.5
  #   depends_on:
  #     thingsboard:
  #       condition: service_healthy
  #   networks:
  #     - thingsboard

  ws-events:
    build:
      context: ./send-telemetry/
      dockerfile: ws-events.Dockerfile
    image: ws-events:3.7-alab
    container_name: ws-events
    mem_limit: 512m
    cpus: 0.5
    depends_on:
      thingsboard:
        condition: service_healthy
    networks:
      - thingsboard
    volumes:
      - .:/app

  dashboard-script:
    build:
      context: ./send-telemetry/
      dockerfile: dashboard-script.Dockerfile
    image: dashboard-script:3.7-alab
    container_name: dashboard-script
    # mem_limit: 512m
    # cpus: 0.5
    depends_on:
      thingsboard:
        condition: service_healthy
    volumes:
      - .:/app
    networks:
      - thingsboard

  model:
    build:
      context: ./predictive-maintenance
      dockerfile: .Dockerfile
    image: model:3.7-alab
    container_name: model
    # mem_limit: 2g
    # mem_reservation: 512mb
    # cpus: 0.75
    volumes:
      - .:/app
      - ../thingsboard_pip_cache:/root/.cache/pip
      - ../thingsboard_models_cache:/models
    depends_on:
      - database
    ports:
      - "8000:8000"
    networks:
      - thingsboard
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 2m
      timeout: 10s
      retries: 3
      start_period: 60s
    env_file:
      - ./.env
    environment:
      - WAIT_HOSTS=database:5432
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database:5432/${POSTGRES_DB}
      - AccountSid=${ACCOUNT_SID:-defaultSid}
      - AccountToken=${ACCOUNT_TOKEN:-defaultToken}
      - TwilioSmsFrom=${TWILIO_SMS_FROM:-+1234567890}
      - PhoneNumberID=${PHONE_NUMBER_ID:-123456789}
      - WB_TOKEN=${WB_TOKEN:-defaultWBToken}
      - EMAIL_ADDRESS=${EMAIL_ADDRESS:-test@example.com}
      - APP_PASSWORD=${APP_PASSWORD:-defaultPassword}
      - MODELS_PATH=/models
      - AUTO_START_PREDICTION_JOBS=${AUTO_START_PREDICTION_JOBS:-false}
      - ENABLE_FILE_WATCH=false
      - LOG_LEVEL=${LOG_LEVEL:-INFO}

  thingsboard:
    build:
      context: .
      dockerfile: .Dockerfile
    image: thingsboard:3.7-alab
    container_name: thingsboard
    # mem_limit: 3g
    # mem_reservation: 512m
    # cpus: 2.0
    volumes:
      - .:/app
      - ../thingsboard_m2_cache:/root/.m2
      - ../thingsboard_npm_cache:/root/.npm
      - ../thingsboard_gradle:/root/.gradle
    ports:
      - "8080:8080"
      - "1884:1884"
      - "7071:7071"
      - "8883:8883"
    networks:
      - thingsboard
    depends_on:
      database:
        condition: service_healthy
      model:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080 || exit 1"]
      interval: 2m
      timeout: 10s
      retries: 3
      start_period: 120s
    environment:
      - WAIT_HOSTS=database:5432,model:8000
      - FASTAPI_BASE_URL=http://model:8000/api/v1/
      - FASTAPI_WS_URL=ws://model:8000/models/ws/unified
      - JAVA_OPTS=-Xms2G -Xmx2G -XX:+UseG1GC -XX:MaxGCPauseMillis=200
      - PM_LOAD_SAMPLE_DATA=true
      - PM_LOAD_SAMPLE_DATA_UPDATE_TO_CURRENT_TIME=true
      - PM_AUTO_LOAD_ON_STARTUP=false
      - PM_TENANT_EMAIL=${PM_TENANT_EMAIL:-tenant@thingsboard.org}
      - PM_MACHINE_IDS=${PM_MACHINE_IDS:-1,11}
      - PM_MAX_MACHINES=${PM_MAX_MACHINES:-10}
      - PM_ROLLBACK_ON_FAILURE=${PM_ROLLBACK_ON_FAILURE:-true}
      - PM_EXIT_ON_FAILURE=${PM_EXIT_ON_FAILURE:-true}
      - PM_END_DATE_MACHINES=${PM_END_DATE_MACHINES:-2015-04-20 06:00:00,2024-12-31 00:00:00}
      - PM_DATA_PATH=${PM_DATA_PATH:-predictive-maintenance/data}

  ui-ngx:
    build:
      context: ui-ngx
      dockerfile: .Dockerfile
    image: ui-ngx:3.7-alab
    container_name: thingsboard-dev
    # mem_limit: 10g
    # mem_reservation: 1g
    # cpus: 1.0
    volumes:
      - .:/app
      - ../thingsboard_npm_cache:/root/.npm
    ports:
      - "4200:4200"
    networks:
      - thingsboard
    depends_on:
      thingsboard:
        condition: service_healthy
    environment:
      - WAIT_HOSTS=thingsboard:8080
      - NODE_OPTIONS=--max-old-space-size=8192
      - CHOKIDAR_USEPOLLING=false
      - CHOKIDAR_INTERVAL=3000
      - NG_CLI_ANALYTICS=false
      - NG_BUILD_CACHE=.angular/cache
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:4200 || exit 1"]
      interval: 3m
      timeout: 30s
      retries: 3
      start_period: 90s

networks:
  thingsboard: