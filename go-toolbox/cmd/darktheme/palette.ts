// ponytail: stub — palette/CSS vars not needed for static file generation
import type { RGBA } from './color';
export function getRegisteredColor(_type: string, _rgb: RGBA): string | null { return null; }
export function registerColor(_type: string, _rgb: RGBA, value: string): string { return value; }
